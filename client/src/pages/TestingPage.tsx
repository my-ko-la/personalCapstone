import CartWidget from "../components/c_global/CartWidget";

const TestingPage = () => {
  return (
    <div className="flex flex-col items-center justify-center h-screen w-screen ">
      <CartWidget username="Myko" />
    </div>
  );
};

export default TestingPage;
